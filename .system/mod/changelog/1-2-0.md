# 1.2.0

**Added**

- **Mouse capture:** `s2c_mouse_capture` + `c2s_mouse_action` — clicks, scroll, optional cursor (0–1); client capability `SERVER_MOUSE_CAPTURE`; Paper + Fabric reference.
- **Version requirements:** config / API strings — `>=` (bare `X.Y.Z` or `X.Y.Z>`), exact `=X.Y.Z`, ceiling `<X.Y.Z`, minor line `X.Y.x` / `X.Y`; all ANDed. Invalid patterns rejected.
- **Minecraft 26.1:** second Fabric JAR `KrepAPI-mc26.1` (mod id `krepapi`, 26.1–26.1.1). `KrepAPI-mc1.21` for 1.21.4–1.21.11. One `gradlew build` → both + Paper ref.

**Fixed**

- `overrideVanilla` follows the player's current keybind (`KeyMapping.matches(KeyEvent)`), not only the server `defaultKey`.

**Changed**

- Two-part `X.Y` in requirements = minor line `X.Y.*` (not `>= X.Y.0`); use `X.Y.0` for a floor.
